# Use the official Nginx image
FROM nginx:alpine

# Copy our HTML file to the directory Nginx uses to serve content
COPY index.html /usr/share/nginx/html/index.html

# The Nginx image automatically starts the server on port 80
